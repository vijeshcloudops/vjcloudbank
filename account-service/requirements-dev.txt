# ─────────────────────────────────────────────
# requirements-dev.txt — Windows local development only
# ─────────────────────────────────────────────
# Use this file on Windows instead of requirements.txt
# to avoid the C++ compilation issue with asyncpg.
#
# Install with:  pip install -r requirements-dev.txt
# Production Docker still uses requirements.txt (asyncpg on Linux)

fastapi==0.109.0
uvicorn[standard]==0.27.0
psycopg2-binary==2.9.9
pydantic==2.5.3
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
httpx==0.26.0
