# requirements-dev.txt
#
# Test-only dependencies. Not installed in production image.
# CI installs these separately after installing requirements.txt.

# Test runner + async support
pytest==8.0.0
pytest-asyncio==0.23.5

# Coverage measurement — outputs xml (Sonar) + html
pytest-cov==4.1.0

# For mocking AsyncMock in a clean way (already in stdlib but pin for stability)
# Nothing extra needed — unittest.mock covers async

# HTTP client for future integration tests
httpx==0.26.0
