# VjCloudBank Account Service - Python Dependencies
#
# fastapi      = the web framework (like Express but for Python)
# uvicorn      = the server that runs FastAPI (like nodemon but for Python)
# asyncpg      = async PostgreSQL driver — very fast
# pydantic     = data validation (built into FastAPI)
# python-jose  = JWT token verification
# python-dotenv= loads .env file

fastapi==0.109.0
uvicorn[standard]==0.27.0
asyncpg==0.29.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
httpx==0.26.0
