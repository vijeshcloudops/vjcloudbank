apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vjcloudbank-transaction-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/vijeshcloudops/vjcloudbank.git
    targetRevision: main
    path: manifests/charts/microservice
    helm:
      releaseName: transaction-service
      valueFiles:
        - ../../values/transaction-service-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: vjcloudbank
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
